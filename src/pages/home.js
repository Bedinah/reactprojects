import React from "react";
import MyButton from "../components/button";

const Home = ({ count, onClick }) => {
  return (
    <div>
      <h1>jkkkfddddlkf</h1>
      <MyButton onClick={onClick}>we ve clicked {count} times</MyButton>
    </div>
  );
};

export default Home;
